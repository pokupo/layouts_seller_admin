//- Подключение скриптов
mixin script(scripts)
	each src in scripts
		- src = (/^(https?:)?\/\//.test(src) ? src : 'scripts/' + src) + '?' + timestamp;
		script(src=src)

mixin tools(list)
	ul.list
		each item in list
			li.list__item
				span.mark= item[0]
				= ' - ' + item[1]
